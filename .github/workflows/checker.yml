name: Parameter checker tests

on:
  push:
    paths:
      - 'tools/**'
      - 'app-reverse-engineering.js'
      - 'index.html'
      - '.github/workflows/checker.yml'
  pull_request:
    paths:
      - 'tools/**'
      - 'app-reverse-engineering.js'
      - 'index.html'
      - '.github/workflows/checker.yml'

jobs:
  checker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Python unit tests
        run: python -m unittest tools/test_dlive_re.py tools/test_director_automation.py
      - name: Python syntax
        run: python -m py_compile tools/dlive_re.py tools/director_automation.py
      - name: Browser checker syntax
        run: node --check app-reverse-engineering.js
      - name: Parameter checker CLI help
        run: python tools/dlive_re.py --help
      - name: Director automation dry-run smoke
        run: |
          python tools/director_automation.py list tools/director/echo3-6.toml
          python tools/director_automation.py --help
